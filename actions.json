{
  "_comment": "Contract for bridge actions. `client` is the CLI subcommand when it differs from the action name. `required`/`optional` are the client argparse positional/flag args. Checked against HANDLERS (control.lua) and factorio_ai.py by contract_check.py.",
  "schema_version": 1,
  "actions": {
    "ping":         { "required": [], "optional": [] },
    "snapshot":     { "required": [], "optional": ["surface", "x", "y", "radius", "offset", "limit", "tiles", "name"] },
    "brief":        { "required": [], "optional": ["surface", "x", "y", "radius"], "note": "one reply: owned counts, issues, enemies, ore_total/ore_patches, nearest_water, treasury" },
    "blueprint_export": { "client": "blueprint-export", "required": ["x1", "y1", "x2", "y2", "file"], "optional": ["surface", "force"], "note": "read built area into Factorio blueprint string; client writes a new file" },
    "blueprint_import": { "client": "blueprint-import", "required": ["file", "x", "y"], "optional": ["surface", "force", "ghosts"], "note": "default direct construction debits real treasury items; --ghosts defers construction to robots" },
    "index":        { "required": [], "optional": ["surface"], "note": "whole-map ore/enemy/water index, cached in mod storage" },
    "ore_marks":    { "client": "ore-marks", "required": [], "optional": ["surface", "name", "offset", "limit"], "note": "persistent ore sectors found by index, paged and filtered by resource" },
    "smelt_plan":  { "client": "smelt-plan", "required": ["rate"], "optional": ["surface", "force", "x", "y", "input_x", "input_y"], "note": "size/site/preflight a reusable iron-smelting row in one call; no construction" },
    "smelt_build": { "client": "smelt-build", "required": ["plan_id"], "optional": [], "note": "revalidate, build with real items, export blueprint and start in-game audit" },
    "smelt_status": { "client": "smelt-status", "required": ["plan_id"], "optional": [], "note": "compact result for the exact row, including a 60-game-second production audit" },
    "set_treasury": { "client": "treasury", "required": ["x", "y"], "optional": ["surface"] },
    "craft":        { "required": ["recipe"], "optional": ["count"] },
    "mine":         { "required": ["name", "x", "y"], "optional": ["surface"] },
    "collect":      { "required": ["item", "count", "x", "y"], "optional": ["surface"] },
    "autofuel":     { "required": ["state"], "optional": [], "note": "client arg state on|off maps to action field enabled(bool)" },
    "spec":         { "required": ["kind"], "optional": ["name", "entity", "force"], "note": "kind recipe is force-level: enabled/have/via_entity/unlocked_by" },
    "audit":        { "required": ["item"], "optional": ["surface", "force", "precision", "expected_per_second"], "note": "expected_per_second is client-only, not sent to Lua" },
    "set_recipe":   { "client": "set-recipe", "required": ["recipe", "x", "y"], "optional": ["surface", "force"] },
    "research":     { "required": [], "optional": ["name", "start", "force"] },
    "insert":       { "required": ["item", "count", "x", "y"], "optional": ["surface", "force"], "note": "input slot for lab/assembler/ammo-turret, else fuel slot (burner)" },
    "place":        { "required": ["name", "x", "y"], "optional": ["surface", "force", "direction", "dry_run"], "note": "dry_run reports blockers and builds nothing" }
  }
}
